# Cactus Wave Media

Studio site. Next.js, Tailwind, custom design system.

```bash
npm install
npm run dev
```

Contact form opens a mail draft to `hello@cactuswavemedia.com`. Swap that address in `src/lib/site.ts`. Selected work lives in `src/lib/work.ts` — replace the studies with live projects when you’re ready.
